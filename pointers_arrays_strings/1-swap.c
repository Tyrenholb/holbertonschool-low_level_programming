#include "main.h"
/**
* swap_int - swaps the values of two integers.
* @a: The first pointer for the int a.
* @b: the second pointer for the int b.
*/
void swap_int(int *a, int *b)
{
	temp = *a;
	*a = *b;
	*b = *a;
}
